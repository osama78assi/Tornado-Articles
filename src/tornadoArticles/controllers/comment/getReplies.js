import { Request, Response } from "express";

/**
 *
 * @param {Request} req
 * @param {Response} res
 */
async function getReplies(req, res, next) {
    try {
        
    } catch(err) {
        next(err);
    }
}

export default getReplies;