const { Request, Response } = require("express");

/**
 *
 * @param {Request} req
 * @param {Response} res
 */
async function editArticleContent(req, res, next) {
    try {
        
    } catch(err) {
        next(err);
    }
}

module.exports = editArticleContent;