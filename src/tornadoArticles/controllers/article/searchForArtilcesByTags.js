import { Request, Response } from "express";

/**
 *
 * @param {Request} req
 * @param {Response} res
 */
async function searchForArtilcesByTags(req, res, next) {
    try {
        
    } catch(err) {
        next(err);
    }
}

export default searchForArtilcesByTags;