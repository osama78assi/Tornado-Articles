const { Request, Response } = require("express");

/**
 *
 * @param {Request} req
 * @param {Response} res
 */
async function editArticleTitle(req, res, next) {
    try {
        
    } catch(err) {
        
    }
}

module.exports = editArticleTitle;