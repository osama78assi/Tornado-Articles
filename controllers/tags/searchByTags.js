const { Request, Response } = require("express");

/**
 *
 * @param {Request} req
 * @param {Response} res
 */
async function searchByTags(req, res, next) {
    try {
    } catch (err) {
        next(err);
    }
}

module.exports = searchByTags;
